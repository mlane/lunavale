---
id: react.useEffect
tags: [react, hook, lifecycle]
---

> Runs after the browser paints — use for syncing with the DOM or fetching data.

- Runs only after the paint phase.
- Skip for layout logic — use `useLayoutEffect` instead.
